/**
 * 老师成果台与跨店排名。
 *
 * <p>这个包刻意与 {@code performance}（个人提成明细）分开：那边回答"我这个月赚多少"，
 * 这边回答"我在全国排第几"。两者的数据域也不同 —— 提成严格按门店隔离，
 * 排名必须跨店聚合，靠 {@code ranking:national} 这个单独的权限放行。
 *
 * <p>跨店只返回**名次与本人指标**，不返回别店的明细，所以不破坏门店数据隔离。
 */
package com.jisuodashi.scoreboard;
