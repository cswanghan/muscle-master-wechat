<view class="page">
  <view class="mark">员</view>
  <view class="title">{{title}}</view>
  <view class="hint">技师 / 前台 / 店长 共用此工程 · 前台核销 / 散客收款码</view>
  <navigator url="/pages/frontdesk/frontdesk" class="go">进入前台 iPad</navigator>
  <view class="roles">
    <text wx:for="{{roles}}" wx:key="*this" class="role">{{item}}</text>
  </view>
</view>
