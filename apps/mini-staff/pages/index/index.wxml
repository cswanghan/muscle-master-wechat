<view class="page">
  <view class="mark">员</view>
  <view class="title">{{title}}</view>
  <view class="hint">技师 / 前台 / 店长 共用此工程 · T1/T2 尚未实现</view>
  <view class="roles">
    <text wx:for="{{roles}}" wx:key="*this" class="role">{{item}}</text>
  </view>
</view>
